// Placeholder function — exists only to host integration tests for notification triggers.
// Tests live in ./notifications_test.ts and are executed via `deno test`.
Deno.serve(() => new Response("ok"));
